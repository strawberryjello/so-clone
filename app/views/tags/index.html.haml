%h1 Tags

= link_to 'Questions', questions_path

%hr

= form_tag :search_tags, :method => 'get' do
  %p= label_tag 'Search tags'
  %p= text_field_tag :search_string

  %p= submit_tag 'Search'

%hr

- @tags.each do |t|
  = link_to t.name, tag_path(t)
