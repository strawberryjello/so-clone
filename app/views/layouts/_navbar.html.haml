%nav.navbar.navbar-default
  .container-fluid
    %ul.nav.nav-pills.navbar-left
      %li= link_to 'Ask Question', new_question_path
      %li= link_to 'Questions', questions_path
      %li= link_to 'Tags', tags_path

    %ul.nav.nav-pills.navbar-right
      - if session[:user_id]
        %li= session[:username]
        /= link_to 'Change password', change_password_user_path(session[:user_id])
        %li= link_to 'Logout', logout_path
      - else
        %li= link_to 'Sign Up', new_user_path
        %li= link_to 'Login', login_path
